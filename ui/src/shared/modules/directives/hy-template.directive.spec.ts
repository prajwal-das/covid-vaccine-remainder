import {HyTemplateDirective} from './hy-template.directive';

describe('HyTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new HyTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
